# Infrastructure

No production infrastructure is defined yet.

Use [docker-compose.yml](../docker-compose.yml) only for local dependencies that are needed by active development. Add cloud infrastructure, deployment manifests, image registries, or multi-environment orchestration only after a runnable service or app has a real deployment requirement and an ADR records the decision.
