# Applications

User-facing applications belong here. Each application consumes approved service APIs and shared packages; business rules remain in domain services.

`apps/api-gateway` currently hosts a training/POC HTTP adapter for `POST /v1/work-requests`. See [api-gateway README](api-gateway/README.md). Other app workspaces still export names only.
