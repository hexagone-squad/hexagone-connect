# Applications

User-facing applications belong here. Each application consumes approved service APIs and shared packages; business rules remain in domain services.

The admin portal contains a synthetic, non-production work-qualification queue POC that exercises the existing work-management use case through a narrow adapter.
