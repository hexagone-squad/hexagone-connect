# Claude repository instructions

Follow the repository instructions in `AGENTS.md`. Read
`docs/methodology/CONSTITUTION.md` first, then every applicable glob-routed instruction in
`.github/instructions/`, then local service or package documentation before
changing files. Report only commands actually run and their factual outcomes.
